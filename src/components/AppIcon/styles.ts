import { css } from "iares";

export const styles = () => css`
  app-icon,
  app-icon-type {
    display:flex;
    justify-content: center;
    align-items:center;
  }

  app-icon-type {
    color: var(--green-smooth)
  }
`;
