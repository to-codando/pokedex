import { css } from "iares";

export const styles = () => css`
  app-header {
    height:104px;
  }

  app-header,
  .wrap-ctx {
    display:flex;
    flex-wrap: wrap;
    width:100%;
    background: var(--green-bright);
  }



`;
