import { css } from "iares";

export const styles = () => css`
  app-card,
  .wrap-ctx {
    display: flex;
    width: 100%;
    padding:1em;
    border-radius:8px;
    background: var(--white);
  }
`;
