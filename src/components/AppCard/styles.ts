import { css } from "iares";

export const styles = () => css`
  app-card,
  .wrap-ctx {
    display: flex;
    width: 100%;
    border-radius:8px;
    background: var(--white);
  }

  .wrap-ctx {
    padding:1em;
  }
`;
